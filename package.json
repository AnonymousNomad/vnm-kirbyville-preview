{
  "name": "vnm-kirbyville-preview",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Location-specific website preview for Vape N More — Kirbyville, Texas. Unsolicited design demonstration; indexing disabled until owner authorization.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4187 --strictPort",
    "smoke": "node src/scripts/smoke.js",
    "og": "node src/scripts/og-image.js",
    "verify:browser": "node src/scripts/verify-browser.js",
    "verify": "npm run build && npm run smoke && npm run verify:browser"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0",
    "vite": "^8.3.0"
  }
}
